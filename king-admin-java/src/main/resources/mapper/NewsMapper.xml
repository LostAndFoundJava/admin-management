<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oukingtim.mapper.NewsEditonMapper">

    <select id="selectAll" resultType="com.oukingtim.domain.NewsModel">
        select distinct t.news_category from news t where 1=1 and t.`delete` = 0
    </select>
</mapper>